apiVersion: v1
kind: Secret
metadata:
  name: {{ printf "%s-%s" (include "helper.get_name" $) . }}
  labels:
    app.kubernetes.io/name: {{ include "helper.get_name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
type: Opaque
stringData:
  TOKEN: <path:kv-v2/data/argocd#password>
